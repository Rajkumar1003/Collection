package Task3;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class UseString {
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		String a = "capegeminii";
		//char[] val = a.toCharArray();
//		//long count = ar.stream().map(x->x.charAt(9)).count();
		//System.out.println(count);
		//Map<Character,Long> = ar.stream().collect(Collectors.toMap(x->x.getClass(), Collectors.counting()));
		Map<Character,Long> = Arrays.stream(a).col
			
	}

}
